name: Build GAX Forge APK

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-apk:
    name: Build & Release APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.0'
          channel: 'stable'
          cache: true

      - name: Get dependencies
        run: flutter pub get

      - name: Analyze code
        run: flutter analyze --no-fatal-infos

      - name: Build APK (split per ABI)
        run: |
          flutter build apk --release \
            --split-per-abi \
            --no-tree-shake-icons \
            --target-platform android-arm,android-arm64,android-x64

      - name: Build universal APK
        run: flutter build apk --release --no-tree-shake-icons

      - name: Get version
        id: version
        run: |
          VERSION=$(grep 'version:' pubspec.yaml | head -1 | awk '{print $2}')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build Web (HTML Renderer)
        run: |
          flutter create . --platforms=web
          flutter build web --release --web-renderer html --base-href "/GAX-Forge/"
          cd build/web && zip -r ../../gax-forge-web.zip .

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: GAX Forge v${{ steps.version.outputs.version }}
          body: |
            ## GAX Forge - Flutter UI Design Maker
            
            ### Downloads
            - `app-arm64-v8a-release.apk` → Modern 64-bit Android (recommended)
            - `app-armeabi-v7a-release.apk` → Older 32-bit Android
            - `app-release.apk` → Universal APK
            - `gax-forge-web.zip` → Web Build (HTML Renderer)
            
            ### What's New
            - **Web Support:** Added web build using HTML renderer for better performance on mobile browsers.
            - Full canvas drag-drop widget editor
            - Dart code export
            - Material 3 UI
          files: |
            build/app/outputs/flutter-apk/app-arm64-v8a-release.apk
            build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk
            build/app/outputs/flutter-apk/app-x86_64-release.apk
            build/app/outputs/flutter-apk/app-release.apk
            gax-forge-web.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
